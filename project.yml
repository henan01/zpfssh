name: zpfssh
options:
  bundleIdPrefix: com.zpf
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"
  generateEmptyDirectories: true

packages:
  SwiftTerm:
    url: https://github.com/migueldeicaza/SwiftTerm
    from: 1.2.0

targets:
  zpfssh:
    type: application
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - Sources/zpfssh
    resources:
      - Sources/zpfssh/Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zpf.ssh
        MACOSX_DEPLOYMENT_TARGET: 13.0
        SWIFT_VERSION: 5.0
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: Sources/zpfssh/Resources/Info.plist
    dependencies:
      - package: SwiftTerm
    entitlements:
      path: zpfssh.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.network.client: true
        com.apple.security.files.user-selected.read-write: true

schemes:
  zpfssh:
    build:
      targets:
        zpfssh: all
    run:
      config: Debug
    archive:
      config: Release
